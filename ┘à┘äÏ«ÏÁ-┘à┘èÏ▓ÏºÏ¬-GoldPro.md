# ملخص ميزات وتحسينات GoldPro — وثيقة نقل شاملة
(مواصفات سلوكية + معادلات + ملاحظات تنفيذية، لإعادة البناء في أي تطبيق)

> البنية الأم المفترضة: **Event Sourcing** — كل عملية حدث غير قابل للتعديل بهوية فريدة، والحالة (أرصدة/ديون/فواتير/مخزون) تُبنى بإعادة تشغيل الأحداث. الحذف = حدث إلغاء (VOID) فوق الحدث. هذه البنية شرط لعدة ميزات أدناه (السلة، الرجعية، المزامنة الآمنة).

---

## ١) الإدخال الصوتي (المساعد)

**مطابقة الأسماء الحرفية:**
- التطبيع قبل المقارنة: توحيد الهمزات (أإآٱ→ا)، ة→ه، ى→ي، ؤ→و، ئ→ي، حذف التشكيل، **تجريد أداة التعريف بصورها (ال/لل/ل/ا) من أول كل كلمة** («لعايب»≡«العايب»)، ثم **إسقاط الألف كلياً** («عايب»≡«عيب» — كروم يسقط الألف كثيراً).
- بعد التطبيع: **تطابق حرفي فقط — لا مطابقة ضبابية/Levenshtein** (الضبابية طابقت «الفائده» باسم زبون!).
- عند التطابق: يُكتب الاسم **بصيغته المسجلة في الدفاتر** حرفياً + toast «🔁 صُحِّح الاسم: X ← Y».
- عند عدم التطابق: **اقتراح صريح** «فلان غير موجود في أسمائك — زبون جديد؟» (نافذة استئذان توقف الحفظ في الفواتير، وتنبيه في العمليات السريعة).
- **مصادر الأسماء**: كل الدفاتر معاً — الديون + كل أنواع الفواتير (شراء/بيع/دولار/دبي/الخدمات) + السلفيات + سجل العمليات.
- ⚠️ فخ قاتل: إن كان كائن المساعد `const VA = (...)` فهو **لا يظهر على window** — والدوال الأخرى التي تفحص `window.VA` ستجده undefined وتمرر الاسم خاماً بصمت. الحل: `const VA = window.VA = (...)`.

**مفردات اللهجة للعملات/العيارات:**
- لانقو (بكل الرسوم: لانقو/لانڨو/لانغو/لانجو/لينقو) = **ذهب 24**.
- طرونط/ترونط/طرنط/ترنط = **ذهب 730**.
- ترتيب الفحص: دينار → دولار → لانقو → طرونط → «730» الصريحة → «24» الصريحة.

**أمر «مدلي الفائدة»:**
- Regex متسامح مع نطق كروم: `/فا[يئ]د[ةه]|مد ?لي الفا/` (الهاء والتاء، والمسافة في «مد لي»).
- **أولوية قصوى**: يُفحص أول شيء في الموجّه، قبل أي التقاط أسماء (وإلا اختطفته مسارات الزبائن).

**إملاء فاتورة كاملة صوتياً:** الاسم يمر بالمطابق نفسه؛ الأسطر (وزن/عيار) تُملأ؛ اسم غير مطابق = استئذان قبل الحفظ.

---

## ٢) نافذة الفائدة الشهرية (بأمر صوتي)
- **أعلى النافذة:** الفائدة الكلية للشهر (أجرة + تجارة) بلونها.
- **بطاقتان:** فائدة الأجرة/الخدمة وحدها (= مجموع أجرة فواتير الشهر) | فائدة البيع/الشراء وحدها.
- **منتقي شهور** يسرد كل شهور التاريخ (تُشتق من تواريخ الفواتير — «محفوظة» بطبيعة الأحداث).
- **مبدأ الميزان المتطابق للتجارة:** الفائدة تُحسب فقط على `min(ميزان المبيع، ميزان المشترى)` للشهر:
  `الفائدة = الميزان_المتطابق × (متوسط سعر بيع الغرام − متوسط سعر شرائه)`
  — فما بِيع من أرصدة افتتاحية (بلا فاتورة شراء) **لا تدخل فائدته**. توحيد الميزان: كل قطعة بوزن×عيار÷730.
- **بيع دبي يُقوَّم بسعر الأرشيف المحسوب** (انظر §٣) لا بدولاره الخام.
- سطر شفافية تحت البطاقة: «متطابق Xغ × (بيع A − شراء B)/غ».

---

## ٣) حساب سعر الغرام في أرشيف دبي (خلف الستار)
يظهر على بطاقة كل فاتورة بيع دبي في الأرشيف فقط (لا يدخل أي حساب، أدمين فقط):

**المعادلة:**
```
السعر = (إجمالي_دبي$ − الوزن × سعر_الشحن$) × سعر_الدولار ÷ (الوزن ÷ 0.730) ÷ 100
      − (سعر_الذهب_المحلي × 0.001 ÷ 0.730)          ← خصم خسارة التكرير 1‰
ثم تقريب لأقرب 1000 + فواصل آلاف للعرض
```
- **عرف التقريب اليدوي:** القاسم (الوزن÷0.730) يُقرَّب لعُشر أولاً (958.904→958.9) ليطابق حساب صاحب المحل.
- **مصادر صامتة:** إجمالي/وزن/سعر الدولار من فاتورة دبي نفسها (fallback للسعر الحالي للفواتير القديمة)؛ **سعر الشحن $/غ من آخر فاتورة شحن** (حقل أُضيف لنافذة الشحن، يتعبأ بآخر قيمة) مع fallback لحقل إعدادات؛ سعر الذهب من إعداد السعر الحي.
- خسارة التكرير: المكتب يستلم 999/1000 من المرسَل — قيمة الفاقد تُخصم من السعر المعروض.
- إن غاب سعر الشحن: يظهر السعر موسوماً «(بلا شحن)» بدل الاختفاء الصامت.

---

## ٤) الأرشيف
- **حذف كل أزرار تحميل PDF** من صفوف كل الأنواع (ذهب/دولار/دبي/الخدمات) **+ حذف التحميل التلقائي الخفي بعد الحفظ** (كان يُنزّل PDF بعد كل حفظ دولار/دبي/تسوية!). أُبقي زر واتساب (إرسال لا تنزيل).
- **زر 👁 عرض داخلي** مكانها: يفتح الفاتورة في نافذة بيضاء ملء الشاشة بزر ✕ — بقالب كل نوع. **فوري**: هيكل «جارٍ التحضير…» ثم المحتوى في الإطار التالي؛ **صور الفاتورة تُحمَّل خلفياً** وتلحق (لا تحجب الفتح).
- **بحث باسم الزبون** أعلى الأرشيف: يفلتر كل الأقسام معاً حياً، قيمته تنجو من إعادة الرسم، يتعايش مع شرائح الفلترة.
- **شرائح فلترة** بالأنواع + شريحة مستقلة لوصولات القبض.
- **تكبير الخط على الحاسوب** (media ≥900px): الاسم أكبر غليظاً، الشارات والأزرار درجة أوضح.

---

## ٥) دفتر الديون
- **بحث بالاسم** أعلى الجدول: فلترة حية + رسالة «لا نتائج لـ«…»» + نجاة القيمة. (⚠️ إن وُجدت فلترة أدوار: تُطبَّق **فوق** نتيجة البحث لا بدلها).
- ورقة تسوية لكل زبون بأزرار لكل نوع رصيد، وقابلة للقصّ حسب الدور.
- أدوات أدمين: تصحيح رصيد بضبط هدف (حدث DEBT_FIX)، تعديل/دمج اسم زبون (CUST_RENAME يسري على كل الدفاتر رجعياً).

---

## ٦) المزامنة وحلّها (الملحمة الكاملة — 4 جذور حقيقية)
شارة حية: 🟢 متصل / 🟡 غير محفوظ (N) / 🔴 أوفلاين. القيود المعلقة تُعاد كل 25ث + عند online/العودة للتطبيق.

**الجذور التي اصطيدت بالترتيب:**
1. **القابس الزومبي:** بعد سكون الجهاز يبقى اتصال RTDB ميتاً والتطبيق يظنه حياً ← بعد محاولتين فاشلتين: `goOffline()` ثم `goOnline()` برمجياً (مفعول F5 بلا F5) + فوراً عند العودة من الخلفية مع معلّق.
2. **سقوط جلسة المصادقة:** القواعد تشترط auth لكل كتابة؛ الاتصال أخضر والكتابة مرفوضة ← **إعادة دخول صامتة** من بيانات الجلسة المحفوظة: حارس onAuthStateChanged فوري + فحص الجلسة أولاً في التصعيد.
3. **⚡ الجذر الأكبر — undefined في الحمولة:** حقل بقيمة undefined (كان `realW: شرط ? قيمة : undefined`) يجعل Firebase **يرفض القيد كله للأبد** — وكل إعادة محاولة ترسل نفس الحمولة الفاسدة. الحل المزدوج: إصلاح المصدر (نشر شرطي `...(cond?{k:v}:{})`) + **معقّم شامل في بوابة الرفع**: `JSON.parse(JSON.stringify(payload))` يسقط كل undefined قبل الإرسال — يقتل الفصيلة كلها ويحرر العالق رجعياً.
4. **التشخيص الذاتي:** الشارة **قابلة للنقر** ← نافذة: عدد المعلق + حالة الجلسة + **رمز آخر خطأ حرفياً** + محاولة فورية. وبعد دورتَي علاج فاشلتين تنبثق **تلقائياً** («صوّر هذه الرسالة») — هي التي اصطادت جذر undefined من أول ظهور.
- دفع ما بعد التحميل يرفع المعلق **بهويته** — لا ازدواج فواتير مع F5 أبداً.
- أخطاء الدخول ناطقة أيضاً: رمز Firebase + ترجمته العملية (مزوّد معطل/حظر محاولات/كلمة خاطئة فعلاً…).

---

## ٧) سلة المحذوفات (هدية بنية الأحداث)
- الحذف = حدث إلغاء؛ فالمحذوفات **لم تُمحَ قط**.
- زر بالإعدادات ← قائمة بكل الإلغاءات الحية: وصف الهدف (نوع الفاتورة/القيد + الزبون + المبلغ) + تاريخ الحذف.
- **↩️ استرجاع = إلغاءُ الإلغاء**: يعيد العنصر **بكامل أثره المالي** (مخزون/ديون/سيولة) كأن الحذف لم يكن.
- الشرط البنيوي: عند إعادة البناء، **الإلغاءات الحية فقط تُعتمد** — إلغاءٌ استُهدف هو نفسه بإلغاء لا يُعتدّ به. سلسلة حذف↔استرجاع↔حذف تعمل.
- رجعية تلقائياً على كل تاريخ الحذف.

---

## ٨) النسخ الاحتياطي والأمان
- **نسخة تلقائية كل 12 ساعة — جهاز الأدمين فقط** (حارس دور صريح): ملف مشفر AES يحوي متجر الأحداث كاملاً (منه يُبنى كل شيء) لمجلد التنزيلات، بنفس صيغة الاستيراد اليدوي. لا تُلتقط مع قيود معلقة. مفتاح آخر التقاط لكل مستخدم.
- ستارة إقلاع تغطي 3 مسارات كي لا تومض واجهة الأدمين: فتح بجلسة دور مقيد، لحظة تسجيل دخول دور مقيد (قبل تلاشي شاشة الدخول)، وتثبيت جديد بلا جلسة (تُرفع عند جاهزية شاشة الدخول).

---

## ٩) الأدوار والدخول
- **دور مقيد للعرض/التسوية** (إنشاء ذاتي أول دخول بكلمة بذرة): يرى قسم أرشيف واحداً محدداً (👁 عرض فقط — لا تعديل/حذف/مشاركة) + دفتر ديون مفلتراً على أنواعه المسموحة (والدينار يظهر لمن له حساب بالنوع الأساسي) بأزرار تسوية مقصوصة على المسموح فقط؛ شارة اتصال أعلى الوسط؛ **إشعار FCM** له عند كل فاتورة جديدة من قسمه (زر 🔔 يسجل token بمفتاحه، والحفظ يدفع لطابور _push تلتقطه دالة سحابية).
- بطاقة بوابة الزبون: كل فواتيره بأنواعها بشاراتها، حية مع كل تحديث.

---

## ١٠) قراءة الصور الذكية (Vision OCR) لتعبئة الفواتير
زر 📷 واحد: يرفق الصورة + يقرؤها + يملأ سطور (وزن/عيار) عبر نافذة مراجعة. ثلاث طبقات تحليل بالترتيب:
1. **كتل مكانية** (ملصقات سبائك): زوج كل ملصق من كتلته وحدها — يستحيل اقتران عيار ملصق بوزن جاره. **تجريد التواريخ/الساعات أولاً** (21-06-2026 يتفكك لأرقام خادعة)، ولاحقة **g** قرينة وزن قاطعة.
2. **اقتران سطري** (أوراق XRF متعددة): سطر فيه عيار واحد (500–999.9 بكسور) ووزن واحد (<500) = سبيكة؛ **سطور فيها 3+ أرقام من نفس الفئة تُتجاهل** (رؤوس تحويل 705/730/750/1000 وقيمها).
3. صيغ قديمة (Au+Poids…).
- **منطق المدى يحكم دائماً**: عيار 500–999.9، وزن <500 — فلا يُخلط 10.29 عياراً أو 743.6 وزناً مهما كان الترتيب.
- قتل اقتراحات كروم للحقول: **اسم عشوائي جديد لكل رسم** (autocomplete=off وحدها لا تكفي).

---

## ١١) منطق مالي جوهري (قابل للنقل كمبادئ)
- **عائلة «الذهب الافتراضي»:** إن كان للرصيد شقّان (سبائك فيزيائية + رصيد حسابي كتحويل بيع) — فكل حارسٍ ومسارِ استهلاك يجب أن يفحص **المجموع** ويستهلك **الفيزيائي أولاً ثم الافتراضي** بعلمٍ في الحدث يطبقه المُسقِط. (أربع ثغرات متطابقة اصطيدت واحدة واحدة: تحقّق من *كل* المستهلكين).
- **تسامح تقريب العرض:** الدين الفعلي 754.0151 يُعرض 754.02؛ الحارس الصارم يرفض «الكمية أكبر من الدين». الحل: تسامح +0.011 + **قصّ صامت على الفعلي**. (اصطيدت في 4 حراس متشابهين).
- **دفع زائد باستئذان:** أي دفع/تحويل يتجاوز المستحق لا يُرفض — نافذة تشرح بالأرقام أين سيذهب الفارض (يسدد ديناً سابقاً/ينقلب رصيداً معاكساً) ثم يمضي.
- **السلف بمكافئ موحد:** إقراض سبيكة عيار k يقيَّد ديناً = وزن×k÷730 لا بالخام، والنافذة تعرض «يُقيَّد: X» قبل التأكيد.
- **خالص/غير خالص** في كل نافذة تسوية بسعر: خالص = نقد فوري من/إلى السيولة بلا قيد دين؛ والفاتورة تُوسم.
- **حصانة ازدواج بالهوية في المُسقِط** لأي قائمة تُغذى بأحداث إضافة (نفس هوية العنصر لا تدخل مرتين — تحمي من هجرات مزدوجة متعددة الأجهزة، رجعياً).
- **قيد ثمن الشحن:** كل شحنة تقيّد ديناً بالدولار لصالح «شحن» = المكرر × سعر_الشحن$/غ.

---

## ١٢) نظام الإصدار والنشر (لأي PWA)
- **قناري نسخة** في كل ملف JS + **أختام ?v=N** على كل الروابط في index وSW + اسم كاش بنفس الرقم — ترقية ذرّية موحدة بسكربت واحد؛ الشارة تعرض نسخة **الكود العامل** وتحمرّ عند تفاوت الملفات.
- **تحديث ذاتي:** SW جديد ← تفعيل فوري ← reload لطيف عبر controllerchange + فحص كل 5 دقائق وعند العودة.
- منعش الشريط السفلي الثابت (أندرويد يخفيه بعد الكيبورد/الخلفية): إعادة رسم قسرية عند العودة/resize/orientation/بعد الدخول.
- درس متكرر: انزلاق CDN ~10 دقائق — «أغلق وافتح مرتين» والشارة تكشف.


---

## ١٣) بيع دبي — تصحيح الخصم + سعر الصرف المؤرَّخ
- **معادلة واحدة في كل مكان:** `الإجمالي$ = (سعر الشاشة − الخصم) × الوزن ÷ 31.1035` — الخصم **يُطرح** دائماً.
- ⚠️ فخ اصطيد: الطباعة كانت تعتمد قيمة `usd` المخزونة (`d.usd || معادلة`) — قيمة قديمة فاسدة تخلّد الخطأ. الحل: الطباعة **تعيد الحساب دائماً** من (سعر/خصم/وزن) وتتجاهل المخزون ← يصحّح طباعة الفواتير القديمة تلقائياً.
- الدفتر يُقيّد بـ`usd` المخزونة لحظة الحفظ؛ فاتورة قديمة فاسدة تُصحَّح بفتحها → تعديل → حفظ (إبطال + إعادة إنشاء)، و**إعادة البناء تصحّح رصيد المكتب رجعياً** حتى مع معاملات لاحقة (تُطبَّق فوق القيمة المصححة).
- **سعر الصرف يُختم في الفاتورة** (`rate`) لحظة البيع — التحويلات اللاحقة للدينار تستعمل سعر كل فاتورة لا السعر الحالي (fallback للحالي للفواتير قبل الميزة).

## ١٤) حصيلة الفترة الصوتية (يوم/شهر) — الطريقة (أ)
- نداء: فائدة/فايدة/فاىدة/حصيلة/ربح/أرباح (+ «شهر» للشهري) — **التطبيع قبل الفحص**: [إأآٱ]→ا، [ؤئ]→ي، ء→∅، ى→ي، ة→ه (كروم يكتب الهمزات عشوائياً).
- نافذة: مبيعات دبي (Σ `a×(rate||الحالي)` بالدينار + مقابلها $) − مشتريات الذهب − مصاريف (دولارية×السعر الحالي) + مبيعات محلية، ووزن الشحن معلومة. **صافي تقديري** ملوّن + تنويه: الشهري أصدق من اليومي (شراء فترة وبيع أخرى).
- (الطريقة (ب) — تتبّع تكلفة لكل سبيكة لربح حقيقي لكل صفقة — مؤجلة بقرار).

## ١٥) الشحن — سجل مستقل + إخفاء من كشف المكتب
- القيد المالي: المكتب يُقيَّد له **المكرر** `rc = وزن×999/1000` ذهب 24؛ وحساب «شحن» يُقيَّد عليه `rc × سعر$/غ` دولاراً.
- **سطر الشحن يُخفى من كشف/إرسال حساب المكتب** (استثناء `t==='شحن'` في **كل** بناة الكشف — 3 مواضع) والرصيد يبقى صحيحاً (يُبنى من الديون لا من سطور العرض).
- **🚢 سجل شحن مستقل** (زر برأس صفحة السجل): لكل شحنة المكتب/التاريخ/مشحون/استلم (rc أخضر)/السعر/التكلفة rc×p + إجماليات.

## ١٦) ظهور العملية في دفتر الطرف الثاني (نمط السطر المرآتي)
المشكلة العامة: عمليات ثنائية الطرف تقيّد رصيد الطرفين لكن تكتب سطراً لطرف واحد (الكشف يُفلتر بـ`c`).
- **تحويل بين حسابات:** سطر مصدر «تحويل لزبون» + **سطر مرآتي** للهدف «تحويل وارد» (أخضر) `id=evt.id+'_in'` — رجعي تلقائياً.
- **دولار بطرف («من أخذه/المسلَّم»):** سطر مرآتي للطرف «دولار وارد/صادر» `id=evt.id+'_pty'` بتفاصيل (من/إلى + سعر الصرف؛ **الصادر بلا سعر** بطلب صاحب المحل). «دولار صادر» يُضاف لكل مجموعات outTypes (خصم/أحمر).
- ⚠️ **حذف السطر المرآتي:** إبطال `X_in` لا يطابق حدثاً = «حُذف» كذباً. الحل في delOp: **تجريد اللاحقة** `_in|_pty` وإبطال الحدث الأصلي (الطرفان معاً) باستئذان يوضح ذلك.

## ١٧) تصفية الذهب فوق الدين + شراء حر + قيد دفتري
- **تجاوز الدين مسموح:** أُزيل حارس «الكمية أكبر من الدين»؛ `remaining` يُطبَّق بإشارته (`Math.abs>0.001`) ← الرصيد **ينقلب** للاتجاه المعاكس، والنقد يُحسب للكمية كاملة. (مثال معتمد: يسالك 200، تشتري 350 ← تسالو 150 + يسالك ثمن 350).
- **🛒 شراء حر (730/24)** ظاهر دائماً بورقة التسوية حتى برصيد صفر (`forceBuy`): حدث SETTLE_GSM بعلم `freeBuy` — **قيد مستقل يُضاف** (لا يمسّ الرصيد القائم): ذهب +وزن على الزبون، دينار −ثمن عليك، **بلا أي سبائك كوفر**. معاينة صريحة قبل التأكيد («ستدين للزبون بـ…» / «الزبون سيدين لك…») — درس: مصطلحات يسالي/تسالو ملتبسة، **الأرقام والاتجاه الصريح إجباريان**.
- ⚠️ حدود الشراء الحر: لإقفال **رصيد ذهب قائم** استعمل زر التصفية على سطر الذهب (يمسح ويقيّد)، لا الشراء الحر (يضيف قيداً فوقه).
- **📒 قيد دفتري** (صندوق في نافذة قبضت/سلّمت): يسجّل الدين فقط — **لا فحص مخزون، لا سبائك، لا سيولة** (علم `paper` يعطّل B والسبائك في المُسقِط). قبضت+دفتري = دين سالب (تسالو) بلا إدخال كوفر؛ سلّمت+دفتري = دين موجب بلا إخراج. وسم «📒» في تفاصيل السطر. الاستعمال: ترميم قيود مفقودة/أرصدة افتتاحية متأخرة.

## ١٨) حراس سلامة المخزون
- **بيع سبيكة محددة (sbid):** قبل الحفظ يُتحقق أن السبيكة **ما تزال في الكوفر** وبوزن كافٍ (مع استثناء سبائك الفاتورة نفسها عند التعديل — ستُعاد بالإبطال). كانت تُتجاهل بصمت ويُتمّ البيع بلا خصم مخزون.
- **رافيناج بمطابقة الصفوف:** الاستهلاك ليس اقتطاعاً ترتيبياً من المجموع بل **لكل صف سبيكته**: ① تطابق تام (وزن+عيار) ← حذف كامل؛ ② نفس العيار ووزن أقل ← **قصّ جزئي والباقي يحتفظ بعياره**؛ ③ الفائض فقط بالترتيب احتياطاً. (مثال معتمد: 100/760+200/730، أُرسل 99/760+198/198 ← يبقى 1غ/760 + 2غ/730 منفصلتين — لا سبيكة مدموجة بعيار غريب).
- **تعديل فاتورة بأصل مختلف ممنوع:** فاتورة وُلدت من SETTLE_GSM/استيراد ليس لها سبائك؛ تعديلها كفاتورة عادية = إبطال بلا إرجاع + إنشاء بسبائك ← **تضاعف**. الحارس: `_invEventType(id)` — غير INVOICE_BUY/SELL يُمنع برسالة «احذف عمليتها من السجل وأعدها».
- **تحذير الاستهلاك اللاحق موحَّد:** حذف فاتورة **أو** عملية سجل سبائكُها استُهلكت لاحقاً (بيع/رافيناج/شحن — barsRemove/barUpdates بكل صيغها) ← استئذان. القاعدة: **احذف من الأحدث للأقدم**.
- **تدقيق إبطال شامل معتمد:** 19 نوع حدث اختُبر «إنشاء→حذف» على المُسقِط الحقيقي — الحالة تعود **مطابقة بالبايت** في كلها.

## ١٩) قبضت 730 متعدد السبائك + سجل رجعي
- نافذة قبضت مع 730: قائمة سبائك (وزن+عيار لكل سطر، «＋ سبيكة أخرى»)، `finalAmount=Σ w·k/730`، السبائك تدخل الكوفر مفردة، والسطر يعرضها مفصلة «🔸 سبيكة n: وزن·عيار» + «⚖️ الحقيقي».
- ⚠️ فخ: زر الاستلام يمرر 'receive' والفحوص كانت على 'take' — **تطبيع فوري** في المدخل: `gtType=(t==='give')?'give':'take'`.
- **سطر سجل رجعي للأحداث اليتيمة:** أحداث GT قديمة **بلا display.op** (نسخ سابقة) محسوبة رصيداً لكن غائبة عن السجل — المُسقِط **يولّد لها سطراً** من بياناتها (بحقول احتياطية `finalAmount??a`، `gtType??t`، تاريخ من ts) بهوية الحدث نفسه ← تظهر وتُحذف كأي عملية.

## ٢٠) أجرة رافيناج خاصة بالاسم («عثمان»)
- إن كان الاسم بعد إزالة المسافات = «عثمان» حرفياً: الأجرة تُحسب على **مكافئ 730** `Σ وزن×عيار÷730 × السعر` بدل الوزن الحقيقي (100غ/712 ← 97.53 × السعر). حية + حفظ، و`feeW` يُختم بالحدث ويظهر بالفاتورة سطر «وزن الأجرة (مكافئ 730)». الأسماء الأخرى بلا تغيير — والنمط قابل للتعميم كقائمة أسماء أو ☑️ بالفاتورة.

## ٢١) الجلسة والمزامنة والعرض (تكملة §٦ و§١٢)
- **جلسة دائمة:** مفاتيح الجلسة (auth/user/ek) في **localStorage** (كانت sessionStorage تزول بالإغلاق) بقراءة احتياطية للقديمة، والخروج يمسح الاثنين + إعادة دخول Firebase صامتة عند الإقلاع. تنبيه: مفتاح التشفير يقيم بالجهاز — «خروج» إجباري بالأجهزة المشتركة.
- **إعادة رفع تلقائية للمعلق:** كانت عند انتقال أوفلاين→متصل فقط (فشلٌ والاتصال «أخضر» = عالق للأبد) ← الآن: **كل 12ث** + visibilitychange + online(+1.5ث).
- **الحاسوب بلا مساس بالهاتف:** داخل `@media(min-width:1024px)`: جذر rem إلى **17.5px** (وكل شيء يتبعه) و**19px** عند ≥1400، أوزان 700–800 للحقول/الجداول/السجل. 
- ⚠️ **النوافذ الشفافة:** 9 نوافذ ديناميكية استعملت صنف `modal-box` **غير معرّف** في CSS ← تعريف واحد (خلفية/ظل/رأس لاصق/زر إغلاق) أصلحها جميعاً.
- **قناري النسخة العملي:** علامة `• vNN` بعنوان نافذة قبضت مربوطة برقم كاش SW بسكربت ترقية واحد — تشخيص فوري لـ«لم يتحدّث».
- صفوف الفاتورتين تبدأ **6** (كانت 10) + إضافة تلقائية عند ملء وزن الصف الأخير (بلا سرقة تركيز) في الشراء والرافيناج.

## ٢٢) Vision OCR — إضافات تشغيلية (تكملة §١٠)
- **مفتاح مشترك سحابي** `goldpro/_appcfg/visionKey` (قراءة بعد الدخول + كاش محلي): الأدمين يحفظه مرة فيعمل للجميع؛ قيود المفتاح: Cloud Vision API فقط + Application=None (إجباري لـWebView/Median).
- زرّا **📷 كاميرا** (capture=environment) و**🖼️ معرض** لنفس المعالج؛ أخطاء HTTP تُعرض **حرفية كاملة** للتشخيص.
- درس واقعي: «API key not valid» كان نسخاً يدوياً حرّف `AIza`→`Alza` — انسخ من أيقونة النسخ حصراً.
- درس أمان: حذف مفتاح API قديم من Google Cloud وهو **مفتاح التطبيق نفسه** = انقطاع دخول كامل (شاشة «أول مستخدم») — التعافي: مفتاح جديد في firebase.js + إعادة إنشاء حساب Auth تلقائياً عند الدخول (allowCreate) والبيانات سليمة.
